version: "3.9"
services:
  benchmark-suite:
    build:
      context: ..
      dockerfile: Dockerfile
    volumes:
      - ../artifacts:/app/artifacts
      - ../reports:/app/reports
    command: ["python", "-m", "llm_benchmark_suite.cli", "demo", "--output-dir", "artifacts/generated/compose-demo"]
